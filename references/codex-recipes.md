# Codex Recipes

## Improve dense admin table

Use $bootstrap5-transitions.

Improve row update, insert, delete, bulk selection and loading feedback in this Bootstrap 5 table.
This is a dense admin screen.
Prefer safe CSS-only effects.
Preserve Bootstrap classes, `data-bs-*` attributes, ARIA and business logic.
Report selected `bsx-*` classes, metadata, JS requirement and accessibility checks.

## Improve form wizard

Use $bootstrap5-transitions.

Improve step transitions, validation feedback and saving state in this Bootstrap 5 form wizard.
Prefer minimal motion.
Do not add dependencies.
Preserve labels, `aria-describedby`, validation feedback and submit behavior.
