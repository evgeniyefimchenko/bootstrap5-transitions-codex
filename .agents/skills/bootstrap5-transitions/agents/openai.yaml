interface:
  display_name: "Bootstrap 5 Transitions"
  short_description: "Apply accessible Bootstrap 5 UI motion and micro-interactions"
  default_prompt: "Use $bootstrap5-transitions to improve this Bootstrap 5 interface with accessible CSS-first motion. Preserve Bootstrap behavior, ARIA, data-bs-* attributes, and avoid new dependencies."
